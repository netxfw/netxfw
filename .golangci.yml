run:
  timeout: 5m
  issues-exit-code: 1
  tests: true

output:
  formats:
    - format: colored-line-number
  print-issued-lines: true
  print-linter-name: true

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - typecheck
    - gofmt
    - goimports
    - misspell
    - revive
    - gocyclo
    - dupl
    - goconst
    - gocritic
    - prealloc
    - gosec

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: false

  govet:
    enable-all: true
    disable:
      - fieldalignment

  gocyclo:
    min-complexity: 20

  dupl:
    threshold: 150

  goconst:
    min-len: 3
    min-occurrences: 3

  gosec:
    excludes:
      - G115

  revive:
    rules:
      - name: blank-imports
      - name: context-as-argument
      - name: context-keys-type
      - name: dot-imports
      - name: error-return
      - name: error-strings
      - name: error-naming
      - name: if-return
      - name: increment-decrement
      - name: var-naming
      - name: var-declaration
      - name: range
      - name: receiver-naming
      - name: time-naming
      - name: unexported-return
      - name: indent-error-flow
      - name: errorf
      - name: empty-block
      - name: superfluous-else
      - name: unused-parameter
      - name: unreachable-code
      - name: redefines-builtin-id

  gocritic:
    enabled-tags:
      - diagnostic
      - experimental
      - opinionated
      - performance
      - style
    disabled-checks:
      - dupImport
      - ifElseChain
      - octalLiteral
      - whyNoLint
      - wrapperFunc
      - unnamedResult
      - paramTypeCombine
      - hugeParam
      - commentedOutCode

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - dupl
        - gocyclo
        - errcheck
        - revive
        - unusedwrite
        - govet
        - gosec

    - path: test/
      linters:
        - dupl
        - gocyclo
        - errcheck
        - revive
        - unusedwrite
        - govet
        - gosec

    - path: cmd/
      linters:
        - errcheck
        - gosec
        - gocritic

    - path: tools/
      linters:
        - errcheck
        - gosec
        - revive

    - path: pkg/
      linters:
        - gosec
        - revive

    - path: pkg/sdk/mock/
      linters:
        - errcheck

    - path: internal/ports/sdk_compat\.go
      linters:
        - dupl

    - path: internal/i18n/messages_test\.go
      linters:
        - gocritic

    - path: test/performance/baseline_test\.go
      linters:
        - staticcheck

    - path: internal/datapath/xdp/
      linters:
        - errcheck
        - gosec

    - path: internal/app/rule/import_rules\.go
      linters:
        - errcheck

    - path: internal/datapath/xdp/maps/maintenance\.go
      linters:
        - errcheck

    - path: internal/daemon/(agent|dp|standalone)\.go
      linters:
        - errcheck

    - path: internal/app/(ops_util|web_service)\.go
      linters:
        - errcheck

    - path: scripts/
      linters:
        - gocyclo

    - linters:
        - staticcheck
      text: "SA1019:"

    - linters:
        - revive
      text: "exported:"

    - linters:
        - revive
      text: "unused-parameter:"

    - linters:
        - staticcheck
      text: "SA1012:"

  exclude-files:
    - internal/datapath/xdp/backend/netxfw_bpfeb\.go
    - internal/datapath/xdp/backend/netxfw_bpfel\.go

  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
  new: false
